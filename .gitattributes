# Keep repository text byte-stable across operating systems so provenance hashes verify after clone.
* text=auto eol=lf

# Treat generated assets as binary (no diff/merge/EOL conversion).
*.png binary
*.pdf binary
*.docx binary
*.tif binary
*.tiff binary

# Keep shell scripts LF even on Windows.
*.sh text eol=lf
